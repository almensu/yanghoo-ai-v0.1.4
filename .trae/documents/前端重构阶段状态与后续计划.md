## 当前阶段
- 核心重构已完成：服务层统一、路由布局抽象、AIChat受控渲染、WebSocket抽象已落地并通过构建与基本验证。

## 已完成阶段
- 阶段1：API服务层统一（`services/http.js`、`services/api.js`）
- 阶段2：路由与全局布局（`layouts/AppLayout.js` + 嵌套路由于 `App.js`）
- 阶段3：Chat组件受控重构（`features/chat/utils/parseThinking.js`，移除直接DOM操作，受控渲染）
- 阶段4：WebSocket抽象与任务页订阅（`services/ws.js`、`features/tasks/hooks/useTaskUpdates.js`）

## 剩余阶段与数量
- 剩余2个巩固阶段（不改后端契约）：
  1. 阶段5：Chat模块进一步组件化与Hook化（拆分 `MessageList`、`ThinkingBlock`、`ChatInput`，新增 `useChatStream`），覆盖边界与错误提示。
  2. 阶段6：质量保障与优化（单元/组件测试补齐、ESLint/构建告警清理、性能与体验微调）。

## 交付与验收
- 阶段5验收：行为一致、无回归、交互稳定（流式渲染、思考块展示）、错误态友好。
- 阶段6验收：测试覆盖率目标≥70%，零阻塞级告警，构建通过且关键路径无性能退化。

## 实施节奏
- 迭代A：完成阶段5。
- 迭代B：完成阶段6。
- 如需更快，可并行推进，但建议两次提交以降低风险。

## 下一步
- 若确认，按“迭代A→迭代B”的节奏推进；也可要求调整内容或目标覆盖率。