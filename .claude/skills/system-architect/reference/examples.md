# 示例 1 个人站点与应用系统
目标：一个根域名入口，博客与多个应用独立部署，支持灰度与回滚
关键决策：
- 路由策略，子域名优先
- 应用运行在容器平台，静态内容走 CDN
- 统一鉴权层，应用内部只做授权校验

# 示例 2 内容生产流水线系统
目标：输入视频链接，输出可发布长文，具备可追溯与可复用的中间产物
关键决策：
- 分层处理，摄取 清洗 分析 蓝图 写作 编辑
- 每层输出标准化文件，便于验证与回滚
- 关键步骤脚本化，减少模型幻觉与手工返工