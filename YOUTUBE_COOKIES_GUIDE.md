# YangHoo AI - YouTube Cookies 配置指南

## 问题说明

当下载 YouTube 视频或字幕时，可能会遇到以下错误：

```
ERROR: [youtube] VIDEO_ID: Sign in to confirm you're not a bot.
Use --cookies-from-browser or --cookies for the authentication.
```

这是 YouTube 的反机器人保护机制。解决方案是使用浏览器的 cookies 进行认证。

---

## 快速解决方案

### 方法 1：使用浏览器扩展（推荐）

#### 步骤 1：安装扩展
在浏览器中安装 **"Get cookies.txt LOCALLY"** 扩展：

- **Chrome**: [下载链接](https://chrome.google.com/webstore/detail/get-cookiestxt-locally/cclelndahbckbenkjhflpdbgdldlbecc)
- **Firefox**: [下载链接](https://addons.mozilla.org/en-US/firefox/addon/get-cookiestxt-locally/)
- **Edge**: Chrome Web Store 中搜索相同扩展

#### 步骤 2：导出 Cookies
1. 打开 [YouTube](https://www.youtube.com/) 并登录您的账号
2. 访问任意 YouTube 视频页面
3. 点击浏览器工具栏的扩展图标
4. 选择 **"Current Site"** → **"Export"**
5. 下载 `cookies.txt` 文件

#### 步骤 3：放置 Cookies 文件
将下载的 `cookies.txt` 文件放到以下位置之一（按优先级排序）：

```
项目根目录/cookies.txt          ← 最高优先级
~/.config/yt-dlp/cookies.txt
~/cookies.txt
```

**推荐位置**：`backend/cookies.txt`

```bash
# 示例：复制到 backend 目录
cp ~/Downloads/cookies.txt /path/to/yanghoo-ai/backend/
```

#### 步骤 4：验证
重启后端服务，检查日志中是否出现：

```
Using cookies file: /path/to/cookies.txt
```

---

### 方法 2：使用命令行（临时方案）

如果您不想保存 cookies 文件，可以直接使用 yt-dlp 的浏览器 cookies 功能：

```bash
# Chrome
yt-dlp --cookies-from-browser chrome "VIDEO_URL"

# Safari (macOS)
yt-dlp --cookies-from-browser safari "VIDEO_URL"

# Firefox
yt-dlp --cookies-from-browser firefox "VIDEO_URL"
```

**注意**：这种方法只适用于直接使用 yt-dlp 命令行，不适用于通过 API 调用的情况。

---

### 方法 3：手动导出（高级用户）

#### Chrome 用户
1. 打开 `chrome://settings/cookies/detail?site=google.com`
2. 查找并复制以下 cookies 的值：
   - `SID`
   - `HSID`
   - `SSID`
   - `APISID`
   - `SAPISID`
3. 按照 Netscape cookie 格式创建 `cookies.txt`：

```
# Netscape HTTP Cookie File
.youtube.com	TRUE	/	FALSE	1735689600	SID	<您的SID值>
.youtube.com	TRUE	/	FALSE	1735689600	HSID	<您的HSID值>
.youtube.com	TRUE	/	FALSE	1735689600	SSID	<您的SSID值>
.youtube.com	TRUE	/	FALSE	1735689600	APISID	<您的APISID值>
.youtube.com	TRUE	/	FALSE	1735689600	SAPISID	<您的SAPISID值>
```

---

## Cookies 文件格式说明

您的 `cookies.txt` 文件应该类似以下格式：

```
# Netscape HTTP Cookie File
# This file is generated by Get cookies.txt LOCALLY

.youtube.com	TRUE	/	FALSE	1735689600	SID	g.a000j...
.youtube.com	TRUE	/	FALSE	1735689600	HSID	AmQ...
.youtube.com	TRUE	/	FALSE	1735689600	SSID	A...
.youtube.com	TRUE	/	FALSE	1735689600	APISID	e...
```

**字段说明**（制表符分隔）：
1. Domain: `.youtube.com`
2. Include subdomains: `TRUE`
3. Path: `/`
4. Secure: `FALSE` 或 `TRUE`
5. Expiration: Unix 时间戳
6. Name: Cookie 名称
7. Value: Cookie 值

---

## 安全注意事项

### ⚠️ 重要提醒

1. **不要分享 cookies.txt 文件** - 它包含您的登录凭据
2. **定期更新** - Cookies 会过期，需要定期重新导出
3. **使用专用账号** - 建议使用专门的 YouTube 账号用于下载
4. **添加到 .gitignore** - 确保 cookies.txt 不会被提交到版本控制

#### 更新 .gitignore
```bash
echo "cookies.txt" >> .gitignore
echo "backend/cookies.txt" >> .gitignore
```

---

## 验证配置

### 检查 Cookies 是否生效

1. 查看 API 日志：
```bash
tail -f backend.log | grep cookies
```

2. 应该看到类似的日志：
```
Using cookies file: /path/to/backend/cookies.txt
Using cookies file for authentication: /path/to/backend/cookies.txt
```

### 测试下载

通过 UI 或 API 创建新的 YouTube 任务，应该能够成功下载。

---

## 常见问题

### Q: Cookies 多久过期一次？
A: 通常几周到几个月不等。如果下载失败，重新导出 cookies 即可。

### Q: 是否需要登录 Google 账号？
A: 是的，需要在浏览器中登录 YouTube 才能导出有效的 cookies。

### Q: 可以在多台机器上使用同一个 cookies.txt 吗？
A: 可以，但要注意安全问题。

### Q: 为什么还是报错 "Sign in to confirm"？
A: 可能的原因：
1. Cookies 已过期 → 重新导出
2. Cookies 格式不正确 → 检查文件格式
3. Cookies 文件路径不对 → 确认文件位置

---

## 代码实现说明

项目已自动支持 cookies，只需放置 `cookies.txt` 文件即可。

系统会按以下优先级查找 cookies 文件：
1. `backend/cookies.txt`
2. `./cookies.txt` (项目根目录)
3. `~/.config/yt-dlp/cookies.txt`
4. `~/cookies.txt`

修改的相关文件：
- `backend/src/tasks/ingest.py` - 创建任务时使用 cookies
- `backend/src/tasks/download_media.py` - 下载视频时使用 cookies
- `backend/src/tasks/download_youtueb_vtt.py` - 下载字幕时使用 cookies

---

**更新日期**: 2026-01-13
**版本**: v0.1.6
